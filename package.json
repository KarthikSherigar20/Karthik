{
  "devDependencies": {
    "cypress": "^13.17.0",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.2.0",
    "mochawesome-report-generator": "^6.2.0"
  },
  "scripts": {
    "test": "cypress run",
    "generate-report": "mochawesome-merge cypress/reports/*.json -o cypress/reports/report.json && marge cypress/reports/report.json -o cypress/reports",
    "test:regression": "npx cypress run --spec cypress/e2e/Smoke/SearchBen/*.cy.js"
  }
}
